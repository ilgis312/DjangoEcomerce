services:
  web-app:
    restart: always
    hostname: web
    build:
      context: .
    ports:
      - "8000:8000"
    volumes:
      - ./DjangoService:/DjangoService
    command: >
      sh -c "python manage.py migrate && python manage.py runserver 0.0.0.0:8000"
